# README-Style Project Section Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restructure the main project card (Safety Helmet Detection) in both EN and VI locales to display README-style sections with mini-headers and bullet lists, while leaving the two smaller project cards unchanged.

**Architecture:** Add a `ProjectSection` type and make `sections` optional on `ProjectCard`. The component renders sections when present, falls back to `description` otherwise. All new content lives in `portfolio-content.json`.

**Tech Stack:** TypeScript, Next.js, Tailwind CSS — no new dependencies.

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `data/locales.ts` | Modify | Add `ProjectSection` type; make `description` optional on `ProjectCard`; add `sections?` |
| `data/portfolio-content.json` | Modify | Replace `description` on main project card (both EN & VI) with `sections` array |
| `components/sections/ProjectsSection.tsx` | Modify | Render `sections` when present; fall back to `description` |

---

### Task 1: Update TypeScript types

**Files:**
- Modify: `data/locales.ts`

- [ ] **Step 1: Add `ProjectSection` type and update `ProjectCard`**

Open `data/locales.ts`. Replace the existing `ProjectCard` type (lines 28–37) with:

```ts
export type ProjectSection = {
  heading: string;
  body?: string;
  items?: string[];
};

export type ProjectCard = {
  title: string;
  period: string;
  description?: string;
  sections?: ProjectSection[];
  stack: string[];
  outcome: string;
  href?: string;
  hrefHuggingFace?: string;
  demoCaption?: string;
};
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc --noEmit`
Expected: no errors. If errors appear, ensure `description` is marked optional (`description?`) and `sections` is also optional.

- [ ] **Step 3: Commit**

```bash
git add data/locales.ts
git commit -m "feat: add ProjectSection type and make description optional on ProjectCard"
```

---

### Task 2: Add README sections to JSON content

**Files:**
- Modify: `data/portfolio-content.json`

- [ ] **Step 1: Replace EN main project card `description` with `sections`**

In `data/portfolio-content.json`, find the EN `projects.cards[0]` object (the Safety Helmet Detection card). Remove the `"description"` key and add `"sections"` in its place:

```json
"sections": [
  {
    "heading": "Problem Statement",
    "body": "Construction sites require workers to wear safety helmets, but manual monitoring is impractical at scale. Existing CV-based systems struggle with partial occlusion, poor lighting, and distant workers — leading to missed violations and preventable safety risks."
  },
  {
    "heading": "Solution",
    "body": "A two-stage person-first pipeline first detects all persons in frame, then crops the upper body for a specialized helmet classifier — significantly improving recall for occluded or distant workers. Violation reports are auto-generated in Vietnamese via Gemini 2.0 Flash; instant PDF export is available without any LLM dependency."
  },
  {
    "heading": "Key Features",
    "items": [
      "Real-time inference from images and MJPEG video streams",
      "ByteTrack multi-person tracking for consistent identity across frames",
      "Live webcam and IP camera support",
      "Per-alert Vietnamese violation reports generated by Gemini 2.0 Flash",
      "Instant PDF export without LLM dependency",
      "Containerized deployment on Hugging Face Spaces via Docker"
    ]
  },
  {
    "heading": "System Architecture",
    "items": [
      "Stage 1 — YOLOv8n detects all persons in the frame",
      "Stage 2 — Upper-body crop fed into YOLOv8s ONNX for helmet classification",
      "FastAPI backend serves inference endpoints and MJPEG streams",
      "React + TypeScript + Vite frontend with live camera controls",
      "ByteTrack tracker maintains consistent person IDs across frames"
    ]
  },
  {
    "heading": "Results & Achievements",
    "items": [
      "mAP@50: 0.9778 · Recall: 0.9515",
      "Peer-reviewed paper accepted at the 2nd SSRC Conference, IUH 2025",
      "Graduation score: 9.4 / 10 (A+)",
      "Promising Research Award — 2nd SSRC, IUH 2025"
    ]
  }
]
```

- [ ] **Step 2: Replace VI main project card `description` with `sections`**

In `data/portfolio-content.json`, find the VI `projects.cards[0]` object. Remove `"description"` and add `"sections"`:

```json
"sections": [
  {
    "heading": "Đặt vấn đề",
    "body": "Các công trường xây dựng yêu cầu công nhân phải đội mũ bảo hộ, nhưng việc giám sát thủ công là không khả thi ở quy mô lớn. Các hệ thống CV hiện có gặp khó khăn với tình trạng bị che khuất, ánh sáng kém và công nhân ở xa — dẫn đến bỏ sót vi phạm và nguy cơ mất an toàn đáng lẽ có thể phòng ngừa."
  },
  {
    "heading": "Giải pháp",
    "body": "Pipeline AI hai giai đoạn theo hướng person-first: đầu tiên phát hiện toàn bộ người trong khung hình, sau đó cắt phần thân trên để đưa vào bộ phân loại mũ bảo hộ chuyên biệt — cải thiện đáng kể recall cho các trường hợp bị che khuất hoặc ở xa. Báo cáo vi phạm tự động được tạo bằng tiếng Việt qua Gemini 2.0 Flash; xuất PDF tức thì không cần LLM."
  },
  {
    "heading": "Tính năng nổi bật",
    "items": [
      "Phát hiện thời gian thực từ ảnh và stream video MJPEG",
      "ByteTrack theo dõi đa đối tượng, duy trì danh tính nhất quán qua các frame",
      "Hỗ trợ webcam trực tiếp và camera IP",
      "Tự động tạo báo cáo vi phạm tiếng Việt theo từng cảnh báo qua Gemini 2.0 Flash",
      "Xuất PDF tức thì không cần LLM",
      "Triển khai container hóa trên Hugging Face Spaces qua Docker"
    ]
  },
  {
    "heading": "Kiến trúc hệ thống",
    "items": [
      "Giai đoạn 1 — YOLOv8n phát hiện tất cả người trong khung hình",
      "Giai đoạn 2 — Crop phần thân trên đưa vào YOLOv8s ONNX để phân loại mũ",
      "FastAPI backend phục vụ các inference endpoint và MJPEG stream",
      "Frontend React + TypeScript + Vite với điều khiển camera trực tiếp",
      "ByteTrack tracker duy trì ID nhất quán cho từng người qua các frame"
    ]
  },
  {
    "heading": "Kết quả & Thành tựu",
    "items": [
      "mAP@50: 0.9778 · Recall: 0.9515",
      "Bài báo được chấp nhận tại Hội nghị SSRC lần 2, IUH 2025",
      "Điểm đồ án tốt nghiệp: 9.4 / 10 (A+)",
      "Giải Tiềm năng — Hội nghị SSRC lần 2, IUH 2025"
    ]
  }
]
```

- [ ] **Step 3: Verify TypeScript compiles**

Run: `npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add data/portfolio-content.json
git commit -m "feat: add README-style sections to main project card (EN + VI)"
```

---

### Task 3: Update component rendering

**Files:**
- Modify: `components/sections/ProjectsSection.tsx`

- [ ] **Step 1: Replace `card.description` render block with sections-aware logic**

In `components/sections/ProjectsSection.tsx`, find the paragraph that renders `card.description` (currently around line 70–72):

```tsx
<p className="text-sm leading-7 text-[var(--muted)] [text-align:justify]">
  {card.description}
</p>
```

Replace it with:

```tsx
{card.sections ? (
  <div className="space-y-4">
    {card.sections.map((section) => (
      <div key={section.heading}>
        <p className="mb-1.5 font-[var(--font-mono)] text-[0.65rem] font-semibold uppercase tracking-[0.18em] text-[var(--accent)]">
          {section.heading}
        </p>
        {section.body && (
          <p className="text-sm leading-7 text-[var(--muted)] [text-align:justify]">
            {section.body}
          </p>
        )}
        {section.items && (
          <ul className="space-y-1">
            {section.items.map((item) => (
              <li key={item} className="flex gap-2 text-sm leading-7 text-[var(--muted)]">
                <span className="mt-[0.35rem] h-1.5 w-1.5 shrink-0 rounded-full bg-[var(--accent)]" />
                <span>{item}</span>
              </li>
            ))}
          </ul>
        )}
      </div>
    ))}
  </div>
) : (
  <p className="text-sm leading-7 text-[var(--muted)] [text-align:justify]">
    {card.description}
  </p>
)}
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 3: Start dev server and visually verify**

Run: `npm run dev`

Check in browser:
1. Main project card (Safety Helmet Detection) shows 5 sections with mono uppercase headings in accent color and bullet points with dot markers.
2. The two smaller cards (BLIP, Hand Gesture) still show their `description` paragraph unchanged.
3. Switch language (EN ↔ VI) — both versions show the correct sections.
4. `stack` badges, `outcome` block, and demo images remain in their original positions.

- [ ] **Step 4: Commit**

```bash
git add components/sections/ProjectsSection.tsx
git commit -m "feat: render README-style sections in main project card"
```
